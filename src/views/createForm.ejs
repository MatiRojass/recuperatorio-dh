<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
			crossorigin="anonymous"
		/>

		<title>Peliculas</title>
	</head>

	<body>
		<%- include('./partials/header') %>

		<main class="mx-4 my-4">
			<h2>Crear Peliculas</h2>
			<form class="mx-5" action="/create" method="POST">
				<div class="mb-3">
					<label for="title" class="form-label">Titulo</label>
					<input type="text" class="form-control" id="title" name="title"
					placeholder="Ej: Hotel Transilvania" value="<%= locals.old &&
					old.title ? old.title : "" %>"> <% if(locals.errors &&
					errors.title){%>
					<small style="color: red"> <%= errors.title.msg %> </small>
					<% } %>
				</div>
				<div class="mb-3">
					<label for="rating" class="form-label">Rating</label>
					<input type="text" class="form-control" id="rating" name="rating"
					value="<%= locals.old && old.rating ? old.rating : "" %>"> <%
					if(locals.errors && errors.rating){%>
					<small style="color: red"> <%= errors.rating.msg %> </small>
					<% } %>
				</div>
				<div class="mb-3">
					<label for="awards" class="form-label">Premios</label>
					<input type="number" class="form-control" id="awards"
					name="awards"value="<%= locals.old && old.awards ? old.awards : ""
					%>"> <% if(locals.errors && errors.awards){%>
					<small style="color: red"> <%= errors.awards.msg %> </small>
					<% } %>
				</div>
				<div class="mb-3">
					<label for="release_date" class="form-label">Fecha de estreno</label>
					<input
						type="date"
						class="form-control"
						id="release_date"
						name="release_date"
					/>
				</div>
				<div class="mb-3">
					<label for="length" class="form-label">Duracion</label>
					<input type="number" class="form-control" id="length" name="length"
					placeholder="Ej: 120"value="<%= locals.old && old.length ? old.length
					: "" %>"> <% if(locals.errors && errors.length){%>
					<small style="color: red"> <%= errors.length.msg %> </small>
					<% } %>
				</div>
				<div class="mb-3">
					<label for="genres">Genero</label>
					<select
						class="form-select"
						aria-label="Default select example"
						name="genre"
					>
						<% for( let i = 0; i < genres.length; i++ ) { %>
						<option value="<%= genres[i].id %>"><%= genres[i].name %></option>
						<% } %>
					</select>
				</div>
				<button type="submit" class="btn btn-primary">Crear</button>
			</form>
		</main>
		<hr />

		<script
			src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
			integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
			crossorigin="anonymous"
		></script>
	</body>
</html>
